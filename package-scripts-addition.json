{
  "scripts": {
    "build:frontend": "vite build",
    "build:server": "esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist --define:import.meta.dirname='\"dist\"'",
    "build:production": "npm run build:frontend && npm run build:server"
  }
}